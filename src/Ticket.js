function Ticket({ ticketData }) {
  // Check if ticketData is defined before accessing its properties
  if (!ticketData) {
    return null; // or some other fallback content
  }
}

export default Ticket;
